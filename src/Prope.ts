import React from "react";

export type Prope = {   
    id : number,
    name : string,
    description:string,
    category : string,
    isActive: boolean;
}

export type stud = {   
    id : number,
    name : string,
    surName:string,
    studentNo : number,
    birtday : string,
    isActive: boolean;
}